import React from 'react';

// Connects to: User Service — GET /users/:id, PUT /users/:id
const UserManager: React.FC = () => {
  return (
    <div>
      <h1>User Manager</h1>
    </div>
  );
};

export default UserManager;

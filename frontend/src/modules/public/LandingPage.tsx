import React from 'react';

// Reads static/cached data — no direct backend service connection required
const LandingPage: React.FC = () => {
  return (
    <div>
      <h1>Welcome to the Testing Platform</h1>
    </div>
  );
};

export default LandingPage;

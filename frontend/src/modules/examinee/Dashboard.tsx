import React from 'react';

// Connects to: User Service (GET /users/:id), Grading Service (analytics)
const Dashboard: React.FC = () => {
  return (
    <div>
      <h1>Dashboard</h1>
    </div>
  );
};

export default Dashboard;
